<%= form_with(model: snap, local: true) do |form| %>
  <% if snap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snap.errors.count, "error") %> prohibited this snap from being saved:</h2>

      <ul>
      <% snap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :snap_url %>
  </div>

  <div class="field">
    <%= form.label :caption %>
    <%= form.text_area :caption, id: :snap_caption %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
